# Known non-specialist runtime dependencies for the Phase 7 release candidate.
# Keep specialist model dependencies in the project's validated ML environment.
-r backend/requirements.txt
Flask>=3.0,<4
numpy>=1.26
Pillow>=10
requests>=2.31
rasterio>=1.3
shapely>=2.0,<3
pyproj>=3.4,<4
scipy>=1.10,<2
earthengine-api>=1.5
